# coding: utf-8

"""
    Qase.io TestOps API v1

    Qase TestOps API v1 Specification.

    The version of the OpenAPI document: 1.0.0
    Contact: support@qase.io
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from qase.api_client_v1.models.test_case_parameter_group import TestCaseParameterGroup
from qase.api_client_v1.models.test_case_parameter_single import TestCaseParameterSingle
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

TESTCASEPARAMETER_ONE_OF_SCHEMAS = ["TestCaseParameterGroup", "TestCaseParameterSingle"]

class TestCaseParameter(BaseModel):
    """
    TestCaseParameter
    """
    # data type: TestCaseParameterSingle
    oneof_schema_1_validator: Optional[TestCaseParameterSingle] = None
    # data type: TestCaseParameterGroup
    oneof_schema_2_validator: Optional[TestCaseParameterGroup] = None
    actual_instance: Optional[Union[TestCaseParameterGroup, TestCaseParameterSingle]] = None
    one_of_schemas: Set[str] = { "TestCaseParameterGroup", "TestCaseParameterSingle" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = TestCaseParameter.model_construct()
        error_messages = []
        match = 0
        # validate data type: TestCaseParameterSingle
        if not isinstance(v, TestCaseParameterSingle):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TestCaseParameterSingle`")
        else:
            match += 1
        # validate data type: TestCaseParameterGroup
        if not isinstance(v, TestCaseParameterGroup):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TestCaseParameterGroup`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in TestCaseParameter with oneOf schemas: TestCaseParameterGroup, TestCaseParameterSingle. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in TestCaseParameter with oneOf schemas: TestCaseParameterGroup, TestCaseParameterSingle. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into TestCaseParameterSingle
        try:
            instance.actual_instance = TestCaseParameterSingle.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into TestCaseParameterGroup
        try:
            instance.actual_instance = TestCaseParameterGroup.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into TestCaseParameter with oneOf schemas: TestCaseParameterGroup, TestCaseParameterSingle. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into TestCaseParameter with oneOf schemas: TestCaseParameterGroup, TestCaseParameterSingle. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], TestCaseParameterGroup, TestCaseParameterSingle]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


